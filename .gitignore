node_modules/
dist/
*.log
.DS_Store
demo/.venv/
demo/.pytest_cache/
demo/__pycache__/
*.tgz
*.tmp-test-*/
.tmp-test-*/
.launch/
# Tests that need a directory under the working directory - source is only ever read
# from inside it - make one per case and name it for the case. Covering the prefix
# rather than each name is what keeps a new test's leftovers out of a commit.
.tmp-*/
OVERNIGHT.md
