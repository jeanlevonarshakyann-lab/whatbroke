{
  "parsers": {
    "pytest": ["pytest", "pytest `--tb=...`"],
    "node --test": ["node --test"],
    "bun test": ["bun test", "bun test `--reporter=junit`"],
    "bun": ["bun"],
    "deno test": ["deno test", "deno test `--reporter=junit`"],
    "deno": ["deno run", "deno check"],
    "deno lint": ["deno lint / deno fmt --check"],
    "deno fmt": ["deno lint / deno fmt --check"],
    "playwright": ["Playwright", "Playwright `--reporter=json` and `junit`"],
    "jest --json": ["jest / jest --json"],
    "jest": ["jest / jest --json", "jest `--reporters=github-actions`"],
    "mocha json": ["mocha --reporter json / json-stream / xunit"],
    "mocha xunit": ["mocha --reporter json / json-stream / xunit"],
    "mocha": ["mocha"],
    "ava": ["ava"],
    "jasmine": ["jasmine"],
    "rubocop": ["RuboCop"],
    "tap": ["tap"],
    "tap-text": ["bare TAP"],
    "vitest": ["vitest", "vitest `--reporter=junit` / `github-actions`", "vitest `--reporter=json`", "vitest writing to a file", "vitest --reporter=tap / tap-flat"],
    "unittest": ["unittest"],
    "python": ["Python tracebacks", "`python -m` with nothing to run"],
    "eslint -f json": ["eslint `-f json`"],
    "eslint": ["eslint", "eslint that never ran"],
    "ruff": ["ruff"],
    "pylint": ["pylint", "pylint `-f parseable` / `msvs` / `json` / `json2`"],
    "flake8": ["flake8"],
    "golangci-lint": ["golangci-lint"],
    "markdownlint": ["markdownlint", "markdownlint `--json`"],
    "stylelint": ["stylelint", "stylelint `--formatter unix` / `json` / `compact` / `tap`"],
    "shellcheck": ["ShellCheck", "ShellCheck `-f json` / `json1` / `checkstyle`"],
    "yamllint": ["yamllint", "yamllint `-f github`"],
    "biome": ["Biome", "Biome `--reporter=`"],
    "oxlint": ["oxlint"],
    "black": ["black"],
    "prettier": ["prettier"],
    "sass": ["sass"],
    "less": ["less"],
    "webpack": ["webpack"],
    "babel": ["babel"],
    "swc": ["swc"],
    "pyright": ["pyright", "pyright `--outputjson`"],
    "mypy": ["mypy", "mypy `--output=json`"],
    "cmake": ["CMake", "CMake refusing to start"],
    "terraform": ["Terraform", "terraform refusing a command"],
    "terraform fmt": ["terraform fmt `-check -diff`"],
    "swift test": ["swift test"],
    "swift": ["Swift"],
    "clang": ["GCC/Clang", "clang `-fdiagnostics-format=msvc` / `vi`"],
    "ruby": ["Ruby"],
    "perl": ["Perl"],
    "php": ["PHP"],
    "minitest": ["minitest"],
    "rspec": ["RSpec", "RSpec `-f json`"],
    "junit jvm": ["JUnit XML from the JVM"],
    "jvm": ["javac / Maven / Gradle", "javac in other languages", "Gradle test failures"],
    "dotnet test": ["dotnet test", "dotnet test in other languages", "Microsoft.Testing.Platform in other languages", "dotnet test `--logger trx`"],
    "dotnet": [".NET", "dotnet format `--verify-no-changes`"],
    "phpunit": ["PHPUnit", "PHPUnit `--log-junit` / `--testdox` / `--log-teamcity`"],
    "cargo --message-format=json": ["cargo `--message-format=json`"],
    "cargo": ["cargo test", "cargo build", "cargo clippy"],
    "cargo fmt": ["cargo fmt `--check`"],
    "go vet json": ["go vet `-json`"],
    "go test -json": ["go test -json"],
    "go": ["go test", "go build", "go vet", "go refusing to run"],
    "gofmt": ["gofmt `-d`"],
    "go mod": ["go mod"],
    "esbuild": ["esbuild"],
    "vite": ["vite / rollup"],
    "node": ["Node stack traces"],
    "tsc": ["tsc"],
    "git": ["git"],
    "kubectl": ["kubectl"],
    "docker": ["Docker / BuildKit"],
    "make": ["make"],
    "npm": ["npm", "npm `--json`"],
    "pnpm": ["pnpm", "pnpm's boxed errors"],
    "yarn": ["yarn"],
    "composer": ["composer"],
    "uv": ["uv"],
    "poetry": ["Poetry"],
    "bundler": ["bundle"],
    "pip": ["pip"],
    "generic": ["anything else"]
  },
  "rows without a parser": {
    "ninja": "ninja has no parser of its own: what fails under it is a compiler, which has one",
    "a log stamped by CI": "the stamp comes off every log before any parser reads it"
  },
  "parsers without signals": {
    "flake8": "a finding is a location and a code, and nothing in that is the same from one log to the next",
    "generic": "it reads whatever no other parser could, so every log is its business"
  }
}
