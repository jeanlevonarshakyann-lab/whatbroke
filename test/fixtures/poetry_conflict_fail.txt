Updating dependencies
Resolving dependencies...

Because shop depends on requests (2.31.0) which depends on urllib3 (>=1.21.1,<3), urllib3 is required.
So, because shop depends on urllib3 (1.0), version solving failed.
